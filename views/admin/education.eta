<%~ include('partials/header', { title: 'Education', active }) %>
<section class="cms-page-heading">
    <div>
        <p class="cms-kicker">Education</p>
        <h1>Manage education</h1>
        <p>Edit qualifications, institutions, dates, results, and descriptions.</p>
    </div>
    <div class="cms-page-heading-actions">
        <% if (rows.length) { %>
            <script type="application/json" class="cms-sort-modal-data"><%~ JSON.stringify(rows.map((row) => ({ id: row.id, label: row.qualification, meta: row.institution }))) %></script>
            <button type="button" class="cms-button secondary cms-sort-modal-trigger" data-reorder-url="/admin/education/reorder" data-modal-title="Sort education">
                <i class="fas fa-arrow-down-wide-short" aria-hidden="true"></i><span>Sort order</span>
            </button>
        <% } %>
        <a href="/admin/education/new" class="cms-button primary"><i class="fas fa-plus"></i><span>New Education</span></a>
    </div>
</section>

<section class="cms-panel">
    <div class="cms-table-wrap" data-selectable="false">
        <table class="cms-table">
            <thead><tr><th>Order</th><th>Qualification</th><th>Institution</th><th>Duration</th><th></th></tr></thead>
            <tbody>
                <% rows.forEach((row) => { %>
                    <tr>
                        <td><%= row.display_order %></td>
                        <td><strong><%= row.qualification %></strong><span><%= row.field %></span></td>
                        <td><%= row.institution %></td>
                        <td><%= row.duration_start %> - <%= row.duration_end %></td>
                        <td class="cms-table-actions">
                            <a href="/admin/education/<%= row.id %>/edit" class="cms-icon-button" aria-label="Edit <%= row.qualification %>"><i class="fas fa-pen"></i></a>
                            <form method="post" action="/admin/education/<%= row.id %>/delete" onsubmit="return confirm('Delete this education item?')">
                                <button class="cms-icon-button danger" type="submit" aria-label="Delete <%= row.qualification %>"><i class="fas fa-trash"></i></button>
                            </form>
                        </td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </div>
</section>
<%~ include('partials/footer') %>
